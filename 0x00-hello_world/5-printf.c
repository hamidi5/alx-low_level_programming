#include <stdio.h>

/**
 * main - this function prints a string .
 *
 * Return: if the program works the returne is 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
